# {{PROJECT_NAME}}

This is a single-module MoonBit wasm-gc image lab managed by MoonBit Cloud.

The UI host lives in `public/`, and the MoonBit module exports the pixel-processing core that becomes `app.wasm`.
