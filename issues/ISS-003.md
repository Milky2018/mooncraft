# ISS-003: Add Runtime Service supervisor

## Metadata
- Type: feature
- Status: open
- Priority: 1
- Labels: runtime, docker, backend, supervisor
- Assignee: unassigned
- Created: 2026-05-27
- Updated: 2026-05-27
- External ref: none

## Description
Add the control-plane supervisor that manages one project-scoped Runtime Service container per project using Runtime Protocol v3.

## Design
Start Runtime images through their default entrypoint, attach them to a MoonCraft-managed internal Docker network, and do not publish ports. Mount Docker named volumes at `/workspace` and `/home/mooncraft`. Track `stopped`, `ready`, and `running` Runtime Service states per project. Implement lazy recovery and idle TTL stop behavior with a default of 10 minutes and admin-configurable TTL.

## Acceptance Criteria
- [ ] Control-plane can ensure a project's Runtime Service is running and healthy through `GET /health`.
- [ ] Runtime containers use Docker named volumes for `/workspace` and `/home/mooncraft`.
- [ ] Runtime containers are reachable by control-plane over an internal Docker network without host-published ports.
- [ ] Runtime Service state and idle TTL behavior are persisted or recoverable enough for normal server operation.
- [ ] Project switching and browser unload do not directly stop Runtime Services.

## Relationships
- Depends on: ISS-002
- Parent: ISS-001
- Related: ISS-007
- Discovered from: none

## Notes
- 2026-05-27: This replaces the per-run `docker run --rm` executor model.

## Close Notes

