# ISS-013: Initialize Runtime Services through POST /init

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: runtime, backend, source-host, secrets
- Assignee: unassigned
- Created: 2026-05-30
- Updated: 2026-06-01
- External ref: docs/runtime-protocol/openapi.v3.yaml

## Description
Add the Runtime Service initialization flow required by ADR-0007. MoonCraft must initialize a Runtime Service with source repository access and Runtime Secrets before exec, preview, status, or event operations.

## Design
After the Runtime Launcher provides a reachable Runtime Service, call `GET /health`; then call `POST /init` with project id, Project Source Repository URL/default branch/current commit, a short-lived Project Source Credential, and resolved Runtime Secrets. Treat `not_initialized` as a normal pre-init service state. Retry `/init` when safe, but never reinitialize while a Run is active.

## Acceptance Criteria
- [x] Runtime Service ensure flow calls `POST /init` before `/exec`, `/runs`, `/runs/{run_id}/events`, or `/preview/`.
- [x] `/init` payload uses source repository fields, source credential fields, and opaque Runtime Secret name/value payloads.
- [x] Runtime Secrets are no longer materialized through Docker env/file targets in the active protocol path.
- [x] Active Run conflicts from `/init` map to a clear user/admin error.
- [x] Tests cover healthy uninitialized service, successful init, init retry, active-run conflict, and secret resolution.

## Relationships
- Depends on: ISS-011, ISS-012
- Parent: ISS-010
- Related: ISS-014, ISS-015
- Discovered from: none

## Notes
- 2026-05-30: Project Source Credential is a first-class protocol input, not a Runtime Secret.
- 2026-06-01: Started implementation. Existing gateway starts a Docker Runtime Service and checks `/health`, but still injects runtime secrets through the Docker env file and never calls `/init`.

## Close Notes
- 2026-06-01: Added Runtime Service `/init` request/response DTOs and client call, made the Docker gateway call `/init` after health before returning an endpoint, moved Runtime Secrets out of Docker env files and into `/init`, derived source credentials from the project owner's GitHub OAuth token, and updated the official runtime service to expose `not_initialized`, accept `/init`, gate business endpoints before init, and consume API-key secrets from the init payload. Verification: `moon check --warn-list +73`, `moon test --warn-list +73`, and `moon check --warn-list +73` in `docker/agent-runtime/runtime-service`.
