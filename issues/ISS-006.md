# ISS-006: Build the official Runtime Protocol v3 image

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: runtime, docker, official-runtime
- Assignee: unassigned
- Created: 2026-05-27
- Updated: 2026-05-27
- External ref: none

## Description
Create an official Runtime image that implements Runtime Protocol v3. The image should hide any Codex, Claude, model, provider, or account details behind its Runtime Service implementation.

## Design
Add a small HTTP Runtime Service inside the runtime image. It listens on `8080`, implements `/health`, `/exec`, `/runs/{run_id}`, `/runs/{run_id}/events`, and `/preview`, runs the underlying builder internally, emits structured events, owns preview startup on `4792`, and reads any required credentials from v3 semantics-free secrets. The image default entrypoint starts the service.

## Acceptance Criteria
- [x] The official image starts a Runtime Service on `0.0.0.0:8080`.
- [x] The image can accept a prompt through `/exec` and produce a terminal Run Result.
- [x] The image can serve a generated app preview on `0.0.0.0:4792`.
- [x] Agent/model/provider details are not represented in the MoonCraft Runtime Manifest.
- [x] Multi-architecture build and publish flow is updated for the v3 image.

## Relationships
- Depends on: ISS-002
- Parent: ISS-001
- Related: ISS-003, ISS-004, ISS-005
- Discovered from: none

## Notes
- 2026-05-27: The first implementation may use the existing official runtime tooling internally, but that must stay behind the Runtime Service HTTP boundary.

## Close Notes
Implemented the official v3 Runtime Service as a native MoonBit binary in `docker/agent-runtime/runtime-service`, made the image default entrypoint start that service, exposed ports `8080` and `4792`, updated image metadata/default tag to `0.4.0`, refreshed admin/deployment/protocol docs, and updated image build/check/smoke scripts for v3 manifests. The final runtime layer now includes Codex CLI `0.128.0`, Python 3, MoonBit, C build tools, skills, and templates, but not Node.js, npm, npx, or Claude.

Validation:

- `moon -C docker/agent-runtime/runtime-service check --target native --warn-list +73`
- `moon -C docker/agent-runtime/runtime-service build --target native --release`
- `bash -n scripts/check_agent_runtime_image.sh scripts/publish_agent_runtime_image.sh scripts/agent_smoke.sh docker/agent-runtime/bin/mooncraft-agent-entrypoint`
- `moon fmt`
- `moon check --warn-list +73`
- `moon test --warn-list +73`
- `moon info`
- `just build-agent-runtime-image`
- `just check-agent-runtime-image docker.io/moonbitcloud/mooncraft-agent-runtime:0.4.0`
- Protocol smoke using fake `codex`: `POST /exec` reached `succeeded`, events were returned, and Runtime preview was served.
- 2026-06-02 update: Runtime app serving now uses `POST /serve`; generated-application content is served through `GET /preview/...`.
