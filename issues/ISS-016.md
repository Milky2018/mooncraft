# ISS-016: Update official Runtime Service for source-host protocol

## Metadata
- Type: feature
- Status: open
- Priority: 1
- Labels: runtime, official-runtime, docker, git
- Assignee: unassigned
- Created: 2026-05-30
- Updated: 2026-05-30
- External ref: docs/adr/0007-use-source-host-backed-runtime-protocol.md

## Description
Update the official Docker-backed Runtime Service to implement the ADR-0007 source-host-backed protocol. It must initialize from a Project Source Repository, use provided credentials, commit and push successful source changes, and serve preview under `/preview/`.

## Design
Install and use `git` inside the Runtime image. Implement `POST /init` by cloning or updating the repository with the provided short-lived credential and opaque Runtime Secrets. Implement `/exec` so successful source-modifying runs commit and push to the default branch, return `ready_commit_sha`, avoid force push, and fail on unresolved conflicts. Serve generated preview content under `/preview/`.

## Acceptance Criteria
- [ ] Official Runtime Service implements `GET /health`, `POST /init`, `POST /exec`, `GET /runs/{run_id}`, `GET /runs/{run_id}/events`, and `/preview/`.
- [ ] Runtime can clone an empty repository and generate the first source commit.
- [ ] Runtime commits and pushes source changes before returning `succeeded`.
- [ ] Runtime returns final `ready_commit_sha` for succeeded Runs.
- [ ] Runtime serves preview under `/preview/` without relying on port `4792`.
- [ ] Runtime conformance smoke covers init, exec, events, commit push, and preview.

## Relationships
- Depends on: ISS-013, ISS-014, ISS-015
- Parent: ISS-010
- Related: ISS-006
- Discovered from: none

## Notes
- 2026-05-30: The Runtime may use prompts, skills, wrappers, or internal scripts to enforce commit/push. MoonCraft only depends on the protocol result.

## Close Notes

