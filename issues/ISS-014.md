# ISS-014: Require Ready Source Commit for successful Runs

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: runtime, runs, source-host, backend
- Assignee: unassigned
- Created: 2026-05-30
- Updated: 2026-06-01
- External ref: docs/runtime-protocol/openapi.v3.yaml

## Description
Update run execution so a source-modifying Run only succeeds after the Runtime Service commits and pushes source changes to the Project Source Repository default branch and returns the final pushed Ready Source Commit.

## Design
Extend Runtime Run result handling to require `ready_commit_sha` on `succeeded`. Store that commit as the project's current Ready Source Commit. Treat missing commit, push conflict, or source repository errors as non-ready terminal failures. The Runtime may create multiple commits internally, but MoonCraft records only the final pushed commit.

## Acceptance Criteria
- [x] `GET /runs/{run_id}` succeeded responses require and parse `ready_commit_sha`.
- [x] MoonCraft stores the final Ready Source Commit on project success.
- [x] Missing `ready_commit_sha` from a succeeded Runtime Run is treated as a protocol error.
- [x] Source conflicts or push failures do not leave the project marked Ready.
- [x] Tests cover success with commit, success missing commit, failure/conflict, and project commit update.

## Relationships
- Depends on: ISS-013
- Parent: ISS-010
- Related: none
- Discovered from: none

## Notes
- 2026-05-30: Runtime-created commits use MoonCraft Source Identity and Runtime Services must not force push; these are Runtime obligations enforced by protocol tests or runtime conformance checks where practical.
- 2026-06-01: Started from the control-plane enforcement layer. The official runtime service now exposes the field in responses but still needs ISS-016 to actually clone, commit, and push source before it can return a real Ready Source Commit.

## Close Notes
- 2026-06-01: Runtime run responses now parse `ready_commit_sha`; a `succeeded` runtime run without it becomes a protocol failure instead of Ready. Successful run finalization stores the final commit on the Project Source Repository metadata. Verification: `moon check --warn-list +73`, `moon test --warn-list +73`, and `moon check --warn-list +73` in `docker/agent-runtime/runtime-service`.
