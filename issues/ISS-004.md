# ISS-004: Execute Runs through Runtime Service HTTP

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: runtime, backend, runs, events
- Assignee: unassigned
- Created: 2026-05-27
- Updated: 2026-05-27
- External ref: none

## Description
Replace control-plane-owned agent CLI execution with Runtime Service HTTP execution. User prompts should create asynchronous Runs through `POST /exec`, poll Run status, poll Run Events, and persist user-visible progress in MoonCraft.

## Design
MoonCraft allocates `run_id`, sends `{ run_id, prompt }`, treats `202 Accepted` as registered and queryable, polls `/runs/{run_id}` and `/runs/{run_id}/events?after=...` every 1 second, and persists events. The Run Result from `/runs/{run_id}` is the authoritative terminal outcome. Remove active Codex/Claude adapter invocation, model passing, prompt files, result files, agent session extraction, and v2 artifact assumptions from the main flow.

## Acceptance Criteria
- [x] User prompts create v3 Runs through `POST /exec`.
- [x] Run status supports `running`, `succeeded`, and `failed` with required messages.
- [x] Run Events are polled with an `after` cursor and persisted for UI history.
- [x] Duplicate `run_id` handling follows v3 idempotency semantics.
- [x] Different `run_id` while a Run is active returns or maps `409 Conflict` correctly.
- [x] v2 agent adapter code is no longer used for the active Runtime flow.

## Relationships
- Depends on: ISS-003
- Parent: ISS-001
- Related: ISS-008
- Discovered from: none

## Notes
- 2026-05-27: This issue owns the user-visible activity/progress path for v3.

## Close Notes
- 2026-05-27: Active Runtime execution now starts via Runtime Service `/exec`, polls `/runs/{run_id}` and `/runs/{run_id}/events?after=...`, persists events into the run activity stream, and treats Runtime Service terminal status as authoritative. The old v2 agent CLI/Docker execution path is retained only for the smoke fake runtime mode.
