# ISS-032: Improve Runtime preview failure diagnostics

## Metadata
- Type: bug
- Status: closed
- Priority: 2
- Labels: runtime, preview, observability
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-02
- External ref: none

## Description
When preview startup fails, users often receive a generic unreachable message. Runtime Service has access to preview startup details, including `mooncraft-preview.sh` existence, permission errors, preview process exit status, readiness timeout, and preview logs, but those details are not consistently exposed through structured Runtime errors or admin-visible diagnostics.

This makes it hard to distinguish Runtime Service unavailable, generated preview unavailable, missing preview script, build/dependency failure, and preview process still starting.

## Design
Preserve structured preview startup failure details inside Runtime Service and expose clear Runtime Error messages to the control-plane. The control-plane should map these into actionable user-facing messages and admin logs without leaking unnecessary internal details to normal users.

Keep preview process ownership in Runtime Service.

## Acceptance Criteria
- [x] Missing `mooncraft-preview.sh` produces a distinct preview failure reason.
- [x] Preview script permission failure produces a distinct preview failure reason.
- [x] Preview process early exit includes exit status and useful log tail in admin-visible diagnostics.
- [x] Preview readiness timeout is distinguishable from Runtime Service unavailability.
- [x] Control-plane user-facing errors remain concise while admin logs preserve the operational cause.

## Relationships
- Depends on: ISS-028
- Parent: none
- Related: ISS-023, ISS-015, ISS-016
- Discovered from: none

## Notes
- 2026-06-02: Discovered while reviewing preview startup and proxy error boundaries.
- 2026-06-02: Existing Runtime Service startup failures already distinguish missing script, chmod failure, early process exit, readiness timeout, and upstream unreachability. Public preview startup now goes through Runtime Service `POST /preview/start`, so Runtime preview errors are API failures while generated-app 404/500/503 responses from `GET /preview/...` are proxied as content.

## Close Notes
- 2026-06-02: Preserved Runtime Service preview startup diagnostics while changing public preview startup to call `POST /preview/start` and keep ordinary generated-app HTTP statuses as proxied content. Runtime Service failure messages continue to include log tail for early exit and timeout. Verified with `moon check --warn-list +73`, `moon test docker/agent-runtime/runtime-service --warn-list +73`, and `moon test services/control-plane --warn-list +73`.
