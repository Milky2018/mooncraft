# ISS-017: Remove legacy Docker volume and workspace archive protocol paths

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: runtime, cleanup, persistence, migration
- Assignee: unassigned
- Created: 2026-05-30
- Updated: 2026-06-01
- External ref: docs/adr/0007-use-source-host-backed-runtime-protocol.md

## Description
Remove active product dependencies on Docker workspace/home volumes, workspace archive persistence, file secret bundles, and fixed Runtime-internal filesystem paths after the source-host-backed flow is implemented.

## Design
Delete or isolate code that creates `mooncraft-workspace-*` and `mooncraft-home-*` volumes for active projects, imports legacy snapshots into Runtime containers, copies file secrets into containers, or treats workspace snapshots as normal Runtime source state. Keep only explicit migration or legacy-inspection paths that are still needed and clearly labeled.

## Acceptance Criteria
- [x] Active project creation no longer creates Runtime workspace/home Docker volumes.
- [x] Active Runtime startup no longer mounts workspace/home volumes or file secret bundles.
- [x] Active flow no longer imports legacy workspace snapshots into Runtime containers.
- [x] Project deletion does not delete user-owned Project Source Repositories.
- [x] Docs and tests no longer describe Docker volumes as authoritative source storage.

## Relationships
- Depends on: ISS-011, ISS-013, ISS-014, ISS-015
- Parent: ISS-010
- Related: ISS-007
- Discovered from: none

## Notes
- 2026-05-30: ADR-0007 rejects Docker volumes and workspace archives as the source persistence boundary.

## Close Notes
2026-06-01: Removed active Runtime Service workspace/home Docker volume creation, mounting, ownership repair, deletion, and legacy snapshot import paths. Runtime Service records now keep only the container identity needed for TTL cleanup. Legacy workspace snapshot restore remains an explicit public migration/test helper, not part of Runtime startup. Verification: `moon check --warn-list +73`, `moon test services/control-plane --warn-list +73`, and `moon test --warn-list +73`.
