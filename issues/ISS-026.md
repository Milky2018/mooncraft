# ISS-026: Avoid reinitializing healthy Runtime Services

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: runtime, control-plane, lifecycle
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-02
- External ref: none

## Description
`RuntimeGateway::ensure_project_runtime_service_with_idle_refresh()` currently calls `POST /init` even when the project Runtime Service container is already running and `GET /health` is OK. The Runtime Protocol exposes health statuses such as `not_initialized`, `ready`, and `running`; the control-plane should use those statuses instead of treating every healthy container as needing initialization.

This makes a preview request or another ensure path capable of reinitializing an already ready Runtime Service, which can refresh secrets, reconfigure agent credentials, and alter the checked-out workspace even though the caller only wanted to ensure availability or refresh the idle TTL.

## Design
Change the control-plane Runtime Service supervisor so healthy containers are handled by structured health status:

- `ready`: return the endpoint and refresh idle TTL when requested.
- `running`: return the endpoint and refresh idle TTL when requested; do not call `/init`.
- `not_initialized`: call `/init`, then mark the service ready.
- unhealthy, missing, or unreachable container: recreate/start it, wait for health, then initialize once.

Do not add command-content heuristics, retry loops, or frontend workarounds. The fix belongs in the Runtime Service lifecycle boundary.

## Acceptance Criteria
- [x] A running healthy Runtime Service with health status `ready` is not sent `POST /init`.
- [x] A running healthy Runtime Service with health status `running` is not sent `POST /init`.
- [x] A running healthy Runtime Service with health status `not_initialized` is sent `POST /init` exactly once by the ensure path.
- [x] A newly created Runtime Service is still initialized before use.
- [x] Idle TTL refresh behavior is preserved for successful ensure calls.
- [x] Tests cover the `ready`, `running`, and `not_initialized` branches.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-013, ISS-016, ISS-027
- Discovered from: none

## Notes
- 2026-06-02: Discovered while reviewing whether healthy Runtime Services still receive `/init` during ensure.
- 2026-06-02: Implementation started. The control-plane should use structured `/health` status to decide whether existing healthy services need `/init`.

## Close Notes
- 2026-06-02: Added structured Runtime Service `/health` client handling and changed `ensure_project_runtime_service_with_idle_refresh()` to initialize only existing healthy services whose status is `not_initialized`. Existing healthy `ready` and `running` services now skip `/init` and only refresh stored ready state and idle TTL. Newly created services still wait for health and run `/init`. Added a whitebox test for the existing-service health status decision. Verified with `moon check --warn-list +73`, `moon test services/control-plane --warn-list +73`, and `moon test --warn-list +73`.
