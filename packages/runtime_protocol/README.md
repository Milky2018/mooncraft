# MoonCraft Runtime Protocol

Shared MoonBit types and protocol-visible rules for Runtime Protocol v3.

This package is the internal source of truth for MoonCraft's Runtime Service DTOs, routes, status values, Runtime Error shape, JSON helpers, and protocol validation. It intentionally does not implement HTTP transport, Docker launching, source-host integration, agent execution, preview serving, or Runtime-private state.
