name = "mooncraft/runtime_protocol"

version = "0.1.0"

preferred_target = "native"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "mooncraft", "runtime", "protocol" ]

description = "Shared Runtime Protocol v3 types and rules for MoonCraft."
