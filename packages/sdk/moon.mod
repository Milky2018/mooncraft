name = "mooncraft/sdk"

version = "0.1.0"

preferred_target = "native"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "moonbit", "cloud", "sdk" ]

description = "Shared types for the MoonCraft platform."